# Renderer/frontend API origin.
# Leave empty when running the OpenStroid desktop app so the UI uses
# first-party `/auth`, `/me`, and `/library` routes on the embedded backend.
# Never point this at `https://cloud.boosteroid.com` or any Boosteroid origin.
# VITE_API_BASE_URL=

# OpenStroid app configuration.
# SERVER_PORT=3001
# UPSTREAM_BASE_URL=https://cloud.boosteroid.com
# SESSION_SECRET=replace-with-a-long-random-secret
# SESSION_COOKIE_NAME=openstroid_session
# SESSION_TTL_SECONDS=2592000
# COOKIE_SECURE=false
# APP_ORIGIN=http://127.0.0.1:3001
